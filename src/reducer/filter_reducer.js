import {
   LOAD_PRODUCTS,
   SET_GRIDVIEW,
   SET_LISTVIEW, 
   SORT_PRODUCTS, 
   UPDATE_SORT, 
   FILTER_PRODUCTS,
   UPDATE_PRODUCTS, 
   CLEAR_FILTERS 
} from '../actions'

const filter_reducer = (state, aciton) => {
  return (
    




    return null
  )
}

export default filter_reducer
